<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Birthday My Love</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=Cairo:wght@300;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #1a0a0a; /* أسود مائل للعنابي */
            --accent-color: #d4af37; /* ذهبي ملكي */
            --text-color: #f8f1f1;
        }

        body {
            margin: 0;
            padding: 0;
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: 'Playfair Display', serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            overflow-x: hidden;
            text-align: center;
        }

        /* تأثير الخلفية المتلألئة */
        .stars {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at center, #2c0e0e 0%, #1a0a0a 100%);
            z-index: -1;
        }

        .container {
            padding: 20px;
            animation: fadeIn 3s ease-in;
        }

        h1 {
            font-size: 3rem;
            letter-spacing: 2px;
            margin-bottom: 30px;
            color: var(--accent-color);
            text-shadow: 0 0 15px rgba(212, 175, 55, 0.5);
        }

        .sub-text {
            font-family: 'Cairo', sans-serif;
            font-size: 1.2rem;
            opacity: 0.8;
            margin-bottom: 50px;
        }

        /* تصميم قسم الباركود */
        .qr-section {
            background: rgba(255, 255, 255, 0.05);
            padding: 30px;
            border-radius: 20px;
            border: 1px solid var(--accent-color);
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            transition: transform 0.3s ease;
        }

        .qr-section:hover {
            transform: scale(1.05);
        }

        .qr-code {
            width: 200px;
            height: 200px;
            background-color: white;
            padding: 10px;
            border-radius: 10px;
            margin: 0 auto 20px;
        }

        .song-title {
            font-style: italic;
            color: var(--accent-color);
            margin-top: 15px;
            display: block;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* قلوب متساقطة خفيفة */
        .heart {
            position: absolute;
            color: var(--accent-color);
            opacity: 0.3;
            animation: float 6s infinite linear;
        }

        @keyframes float {
            0% { transform: translateY(100vh) rotate(0deg); }
            100% { transform: translateY(-10vh) rotate(360deg); }
        }
    </style>
</head>
<body>

    <div class="stars"></div>

    <div class="container">
        <h1>Happy Birthday to my <br> LOVE OF MY LIFE</h1>
        
        <p class="sub-text">هدية بسيطة لروحكِ الجميلة..</p>

        <div class="qr-section">
            <div class="qr-code">
                <img src="https://api.qrserver.com/v1/create-qr-code/?size=200x200&data=https://open.spotify.com/track/7B3z7nZ9S1S6MG49pS3v9C" alt="Sofia by Clairo QR Code" style="width:100%">
            </div>
            <span class="song-title">Scan to listen: Sofia by Clairo</span>
        </div>
    </div>

    <script>
        function createHeart() {
            const heart = document.createElement('div');
            heart.classList.add('heart');
            heart.innerHTML = '❤';
            heart.style.left = Math.random() * 100 + 'vw';
            heart.style.animationDuration = Math.random() * 3 + 3 + 's';
            heart.style.fontSize = Math.random() * 20 + 10 + 'px';
            document.body.appendChild(heart);
            setTimeout(() => { heart.remove(); }, 6000);
        }
        setInterval(createHeart, 500);
    </script>

</body>
</html>